[package]
name = "prism-fuzz"
version = "0.1.0"
edition = "2024"

[dependencies]
libafl       = { version = "=0.15.4", features = ["prelude"] }
libafl_bolts = { version = "=0.15.4", features = ["prelude"] }

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"